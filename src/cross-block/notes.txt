- Using threadfence() prevents syncing issues when num_pairs <= 32, because all servers are on the same warp. For larger, need to use grid.sync().
    - __syncthreads() could also work for just server, but then clients might not be ready to record recv times. 